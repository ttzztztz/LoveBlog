type User implements IUser {
    id: ID!
    username: String!
    password: String!
    role: String!
    signature: String
    createDate: Date!
}

type Blog implements IBlog {
    id: ID!
    author: User!
    title: String!
    content: String!
    createDate: Date!
    Comment: [Comment!]!
}

type Comment implements IComment {
    id: ID!
    author: User!
    content: String
    createDate: Date!
}
